name: med_train
description: "Medical staff training and accreditation system"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  go_router: ^13.0.0
  provider: ^6.1.1
  intl: ^0.18.0
  flutter_dotenv: ^5.1.0
  path_provider: ^2.1.1
  pdf: ^3.10.7
  printing: ^5.11.1
  file_picker: ^10.3.10
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env